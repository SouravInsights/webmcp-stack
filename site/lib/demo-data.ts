// Generated by site/scripts/build-demo-data.mjs from the real
// webmcp-codegen pipeline. Do not edit by hand; re-run the script instead.

export interface DemoTool {
  name: string;
  verb: string;
  path: string;
  summary: string;
  sideEffect: "read" | "write" | "destructive";
  fileName: string;
  fileCode: string;
  findings: Array<{ severity: "warning" | "error"; message: string }>;
}

export const DEMO_SOURCE = {
  project: "Immich",
  blurb: "the open-source photo app",
  specUrl: "https://github.com/immich-app/immich/blob/main/open-api/immich-openapi-specs.json",
};

export const DEMO_TOOLS: DemoTool[] = [
  {
    "name": "get-all-albums",
    "verb": "GET",
    "path": "/albums",
    "summary": "List all albums",
    "sideEffect": "read",
    "fileName": "get-all-albums.webmcp.ts",
    "fileCode": "import { getModelContext, callApi, toolResult, asToolError } from \"./runtime.webmcp\";\n\n// --- webmcp-codegen: generated. Do not edit this region. ---\n/**\n * List all albums\n *\n * Source: GET /albums (openapi). Risk: safe-read.\n * Starts enabled (see executeGetAllAlbums below).\n * Regenerate with: npx @webmcp-stack/codegen generate\n */\n\n/** The exact contract advertised to the agent. Derived from the API spec. Do not hand-edit. */\nexport const getAllAlbumsInputSchema = {\n  \"type\": \"object\",\n  \"properties\": {\n    \"shared\": {\n      \"type\": \"boolean\",\n      \"description\": \"Only return shared albums\"\n    }\n  },\n  \"required\": []\n};\n\n/** What `execute` receives. The browser validates agent input against the schema above. */\nexport type GetAllAlbumsInput = { \"shared\"?: boolean };\n\n/** Safety hints computed by webmcp-codegen. Informational metadata for hosts and UIs. */\nexport const getAllAlbumsHints = {\"readOnlyHint\":true,\"destructiveHint\":false,\"idempotentHint\":true,\"untrustedContentHint\":false} as const;\n\n/** The tool definition, minus `execute` (which is yours, below the marker). */\nexport const getAllAlbumsTool = {\n  name: \"get-all-albums\",\n  title: \"Get All Albums\",\n  description: \"List all albums\",\n  inputSchema: getAllAlbumsInputSchema,\n  annotations: {\n    readOnlyHint: true,\n    untrustedContentHint: false,\n    consequentialHint: false,\n  },\n};\n\n/** The bare request, without the agent-facing result wrapping. Journeys\n * and your own code compose this; executeGetAllAlbums is the agent-facing one. */\nexport async function fetchGetAllAlbums(input: GetAllAlbumsInput, signal?: AbortSignal) {\n  const data = await callApi(\"/albums\", { method: \"GET\", query: { shared: input.shared }, signal });\n  return data;\n}\n\n/**\n * Register this tool with WebMCP. Call it once on page load, or use\n * registerAllTools() from the generated index.ts. Skips quietly when the\n * browser has no WebMCP runtime.\n *\n * Pass an AbortSignal to unregister later: controller.abort().\n */\nexport async function registerGetAllAlbums(signal?: AbortSignal): Promise<void> {\n  const modelContext = getModelContext();\n  if (!modelContext) return;\n  await modelContext.registerTool(\n    {\n      ...getAllAlbumsTool,\n      // The browser has already validated the agent's input against the schema.\n      // A failure returns a readable result; it never throws (see asToolError).\n      execute: async (input, context) => {\n        try {\n          return await executeGetAllAlbums(input as GetAllAlbumsInput, context?.signal);\n        } catch (error) {\n          return asToolError(error);\n        }\n      },\n    },\n    { signal },\n  );\n}\n\n// --- webmcp-codegen: end generated. Your code below survives regeneration. ---\n\n/**\n * What actually happens when the agent calls \"get-all-albums\".\n *\n * Default implementation: calls GET /albums from this page, with the\n * signed-in user's session. Replace it with your app's own API client\n * whenever you like; the contract above never changes.\n *\n * Calls the API on this page's own origin (same-origin by default).\n */\nexport async function executeGetAllAlbums(input: GetAllAlbumsInput, signal?: AbortSignal) {\n  const data = await fetchGetAllAlbums(input, signal);\n  // Make the effect visible: an agent acts while a human watches this\n  // page. If this call changes what is on screen, update the UI here\n  // (navigate, invalidate a query, dispatch an event).\n  return toolResult(data);\n}",
    "findings": [
      {
        "severity": "warning",
        "message": "No output schema: the agent gets unstructured text back and has to guess at the shape. Add a response schema to the contract so agents can use the results reliably."
      }
    ]
  },
  {
    "name": "create-album",
    "verb": "POST",
    "path": "/albums",
    "summary": "Create an album",
    "sideEffect": "write",
    "fileName": "create-album.webmcp.ts",
    "fileCode": "import { callApi, toolDisabled } from \"./runtime.webmcp\";\n\n// --- webmcp-codegen: generated. Do not edit this region. ---\n/**\n * Create an album\n *\n * Source: POST /albums (openapi). Risk: write-confirm.\n * Starts withheld: not registered until you enable it (see registerCreateAlbum below).\n * Regenerate with: npx @webmcp-stack/codegen generate\n */\n\n/** The exact contract advertised to the agent. Derived from the API spec. Do not hand-edit. */\nexport const createAlbumInputSchema = {\n  \"type\": \"object\",\n  \"properties\": {\n    \"albumName\": {\n      \"type\": \"string\",\n      \"description\": \"Album name.\"\n    },\n    \"description\": {\n      \"type\": \"string\",\n      \"description\": \"Description.\"\n    }\n  },\n  \"required\": [\n    \"albumName\"\n  ]\n};\n\n/** What `execute` receives. The browser validates agent input against the schema above. */\nexport type CreateAlbumInput = { \"albumName\": string; \"description\"?: string };\n\n/** Safety hints computed by webmcp-codegen. Informational metadata for hosts and UIs. */\nexport const createAlbumHints = {\"readOnlyHint\":false,\"destructiveHint\":false,\"idempotentHint\":false,\"untrustedContentHint\":false} as const;\n\n/** The tool definition, minus `execute` (which is yours, below the marker). */\nexport const createAlbumTool = {\n  name: \"create-album\",\n  title: \"Create Album\",\n  description: \"Create an album\",\n  inputSchema: createAlbumInputSchema,\n  annotations: {\n    readOnlyHint: false,\n    untrustedContentHint: false,\n    consequentialHint: false,\n  },\n};\n\n/** The bare request, without the agent-facing result wrapping. Journeys\n * and your own code compose this; executeCreateAlbum is the agent-facing one. */\nexport async function fetchCreateAlbum(input: CreateAlbumInput, signal?: AbortSignal) {\n  const data = await callApi(\"/albums\", { method: \"POST\", body: { albumName: input.albumName, description: input.description }, signal });\n  return data;\n}\n\n/**\n * Withheld: this tool is not registered, so agents cannot see or pick\n * it. The registration below stays commented until you enable the tool\n * (uncomment it and the body of executeCreateAlbum, or flip it in the\n * dashboard and regenerate).\n */\nexport async function registerCreateAlbum(signal?: AbortSignal): Promise<void> {\n  void signal;\n  // const modelContext = getModelContext();\n  // if (!modelContext) return;\n  //   await modelContext.registerTool(\n  //     {\n  //       ...createAlbumTool,\n  //       execute: async (input, context) => {\n  //         // Cancellation wins over everything, including the confirmation.\n  //         context?.signal?.throwIfAborted();\n  //         // This tool changes things, so the user is always asked first. The\n  //         // confirmation lives in the generated region: it cannot be edited away.\n  //         const confirmed = await requestUserConfirmation(\n  //           \"Allow the agent to: Create an album\",\n  //         );\n  //         if (!confirmed) {\n  //           return {\n  //             content: [{ type: \"text\", text: \"The user declined this action.\" }],\n  //             isError: true,\n  //           };\n  //         }\n  //         // The browser has already validated the agent's input against the schema.\n  //         // A failure returns a readable result; it never throws (see asToolError).\n  //         try {\n  //           return await executeCreateAlbum(input as CreateAlbumInput, context?.signal);\n  //         } catch (error) {\n  //           return asToolError(error);\n  //         }\n  //       },\n  //     },\n  //     { signal },\n  //   );\n}\n\n// --- webmcp-codegen: end generated. Your code below survives regeneration. ---\n\n/**\n * What actually happens when the agent calls \"create-album\".\n *\n * Default implementation: calls POST /albums from this page, with the\n * signed-in user's session. Replace it with your app's own API client\n * whenever you like; the contract above never changes.\n *\n * Calls the API on this page's own origin (same-origin by default).\n *\n * This tool is write-confirm: it changes things.\n * The user is asked to confirm every call (built into the generated region).\n */\nexport async function executeCreateAlbum(input: CreateAlbumInput, signal?: AbortSignal) {\n  // This tool is withheld: nothing registers it, so agents cannot see\n  // or call it. To enable it, uncomment the request below and the\n  // registration above, and add toolResult to the import.\n  void signal; // passed to fetch once you enable the call below\n  return toolDisabled(\"create-album.webmcp.ts\");\n\n  // const data = await fetchCreateAlbum(input, signal);\n  // return toolResult(data);\n}",
    "findings": [
      {
        "severity": "warning",
        "message": "No input field has a description. Constraints were synthesized from the schema; one line of prose per field in your spec or schema is better."
      }
    ]
  },
  {
    "name": "get-album-info",
    "verb": "GET",
    "path": "/albums/{id}",
    "summary": "Retrieve an album",
    "sideEffect": "read",
    "fileName": "get-album-info.webmcp.ts",
    "fileCode": "import { getModelContext, callApi, toolResult, asToolError } from \"./runtime.webmcp\";\n\n// --- webmcp-codegen: generated. Do not edit this region. ---\n/**\n * Retrieve an album\n *\n * Source: GET /albums/{id} (openapi). Risk: safe-read.\n * Starts enabled (see executeGetAlbumInfo below).\n * Regenerate with: npx @webmcp-stack/codegen generate\n */\n\n/** The exact contract advertised to the agent. Derived from the API spec. Do not hand-edit. */\nexport const getAlbumInfoInputSchema = {\n  \"type\": \"object\",\n  \"properties\": {\n    \"id\": {\n      \"type\": \"string\",\n      \"format\": \"uuid\",\n      \"description\": \"The unique identifier of the album. A UUID.\"\n    }\n  },\n  \"required\": [\n    \"id\"\n  ]\n};\n\n/** What `execute` receives. The browser validates agent input against the schema above. */\nexport type GetAlbumInfoInput = { \"id\": string };\n\n/** Safety hints computed by webmcp-codegen. Informational metadata for hosts and UIs. */\nexport const getAlbumInfoHints = {\"readOnlyHint\":true,\"destructiveHint\":false,\"idempotentHint\":true,\"untrustedContentHint\":false} as const;\n\n/** The tool definition, minus `execute` (which is yours, below the marker). */\nexport const getAlbumInfoTool = {\n  name: \"get-album-info\",\n  title: \"Get Album Info\",\n  description: \"Retrieve an album\",\n  inputSchema: getAlbumInfoInputSchema,\n  annotations: {\n    readOnlyHint: true,\n    untrustedContentHint: false,\n    consequentialHint: false,\n  },\n};\n\n/** The bare request, without the agent-facing result wrapping. Journeys\n * and your own code compose this; executeGetAlbumInfo is the agent-facing one. */\nexport async function fetchGetAlbumInfo(input: GetAlbumInfoInput, signal?: AbortSignal) {\n  const data = await callApi(`/albums/${input.id}`, { method: \"GET\", signal });\n  return data;\n}\n\n/**\n * Register this tool with WebMCP. Call it once on page load, or use\n * registerAllTools() from the generated index.ts. Skips quietly when the\n * browser has no WebMCP runtime.\n *\n * Pass an AbortSignal to unregister later: controller.abort().\n */\nexport async function registerGetAlbumInfo(signal?: AbortSignal): Promise<void> {\n  const modelContext = getModelContext();\n  if (!modelContext) return;\n  await modelContext.registerTool(\n    {\n      ...getAlbumInfoTool,\n      // The browser has already validated the agent's input against the schema.\n      // A failure returns a readable result; it never throws (see asToolError).\n      execute: async (input, context) => {\n        try {\n          return await executeGetAlbumInfo(input as GetAlbumInfoInput, context?.signal);\n        } catch (error) {\n          return asToolError(error);\n        }\n      },\n    },\n    { signal },\n  );\n}\n\n// --- webmcp-codegen: end generated. Your code below survives regeneration. ---\n\n/**\n * What actually happens when the agent calls \"get-album-info\".\n *\n * Default implementation: calls GET /albums/{id} from this page, with the\n * signed-in user's session. Replace it with your app's own API client\n * whenever you like; the contract above never changes.\n *\n * Calls the API on this page's own origin (same-origin by default).\n */\nexport async function executeGetAlbumInfo(input: GetAlbumInfoInput, signal?: AbortSignal) {\n  const data = await fetchGetAlbumInfo(input, signal);\n  // Make the effect visible: an agent acts while a human watches this\n  // page. If this call changes what is on screen, update the UI here\n  // (navigate, invalidate a query, dispatch an event).\n  return toolResult(data);\n}",
    "findings": [
      {
        "severity": "warning",
        "message": "No output schema: the agent gets unstructured text back and has to guess at the shape. Add a response schema to the contract so agents can use the results reliably."
      },
      {
        "severity": "warning",
        "message": "No input field has a description. Constraints were synthesized from the schema; one line of prose per field in your spec or schema is better."
      }
    ]
  },
  {
    "name": "delete-album",
    "verb": "DELETE",
    "path": "/albums/{id}",
    "summary": "Delete an album",
    "sideEffect": "destructive",
    "fileName": "delete-album.webmcp.ts",
    "fileCode": "import { callApi, toolDisabled } from \"./runtime.webmcp\";\n\n// --- webmcp-codegen: generated. Do not edit this region. ---\n/**\n * Delete an album\n *\n * Source: DELETE /albums/{id} (openapi). Risk: destructive-confirm.\n * Starts withheld: not registered until you enable it (see registerDeleteAlbum below).\n * Regenerate with: npx @webmcp-stack/codegen generate\n */\n\n/** The exact contract advertised to the agent. Derived from the API spec. Do not hand-edit. */\nexport const deleteAlbumInputSchema = {\n  \"type\": \"object\",\n  \"properties\": {\n    \"id\": {\n      \"type\": \"string\",\n      \"format\": \"uuid\",\n      \"description\": \"The unique identifier of the album. A UUID.\"\n    }\n  },\n  \"required\": [\n    \"id\"\n  ]\n};\n\n/** What `execute` receives. The browser validates agent input against the schema above. */\nexport type DeleteAlbumInput = { \"id\": string };\n\n/** Safety hints computed by webmcp-codegen. Informational metadata for hosts and UIs. */\nexport const deleteAlbumHints = {\"readOnlyHint\":false,\"destructiveHint\":true,\"idempotentHint\":true,\"untrustedContentHint\":false} as const;\n\n/** The tool definition, minus `execute` (which is yours, below the marker). */\nexport const deleteAlbumTool = {\n  name: \"delete-album\",\n  title: \"Delete Album\",\n  description: \"Delete an album\",\n  inputSchema: deleteAlbumInputSchema,\n  annotations: {\n    readOnlyHint: false,\n    untrustedContentHint: false,\n    consequentialHint: true,\n  },\n};\n\n/** The bare request, without the agent-facing result wrapping. Journeys\n * and your own code compose this; executeDeleteAlbum is the agent-facing one. */\nexport async function fetchDeleteAlbum(input: DeleteAlbumInput, signal?: AbortSignal) {\n  const data = await callApi(`/albums/${input.id}`, { method: \"DELETE\", signal });\n  return data;\n}\n\n/**\n * Withheld: this tool is not registered, so agents cannot see or pick\n * it. The registration below stays commented until you enable the tool\n * (uncomment it and the body of executeDeleteAlbum, or flip it in the\n * dashboard and regenerate).\n */\nexport async function registerDeleteAlbum(signal?: AbortSignal): Promise<void> {\n  void signal;\n  // const modelContext = getModelContext();\n  // if (!modelContext) return;\n  //   await modelContext.registerTool(\n  //     {\n  //       ...deleteAlbumTool,\n  //       execute: async (input, context) => {\n  //         // Cancellation wins over everything, including the confirmation.\n  //         context?.signal?.throwIfAborted();\n  //         // This tool changes things, so the user is always asked first. The\n  //         // confirmation lives in the generated region: it cannot be edited away.\n  //         const confirmed = await requestUserConfirmation(\n  //           \"Allow the agent to: Delete an album\",\n  //         );\n  //         if (!confirmed) {\n  //           return {\n  //             content: [{ type: \"text\", text: \"The user declined this action.\" }],\n  //             isError: true,\n  //           };\n  //         }\n  //         // The browser has already validated the agent's input against the schema.\n  //         // A failure returns a readable result; it never throws (see asToolError).\n  //         try {\n  //           return await executeDeleteAlbum(input as DeleteAlbumInput, context?.signal);\n  //         } catch (error) {\n  //           return asToolError(error);\n  //         }\n  //       },\n  //     },\n  //     { signal },\n  //   );\n}\n\n// --- webmcp-codegen: end generated. Your code below survives regeneration. ---\n\n/**\n * What actually happens when the agent calls \"delete-album\".\n *\n * Default implementation: calls DELETE /albums/{id} from this page, with the\n * signed-in user's session. Replace it with your app's own API client\n * whenever you like; the contract above never changes.\n *\n * Calls the API on this page's own origin (same-origin by default).\n *\n * This tool is destructive-confirm: it cannot easily be undone.\n * The user is asked to confirm every call (built into the generated region).\n */\nexport async function executeDeleteAlbum(input: DeleteAlbumInput, signal?: AbortSignal) {\n  // This tool is withheld: nothing registers it, so agents cannot see\n  // or call it. To enable it, uncomment the request below and the\n  // registration above, and add toolResult to the import.\n  void signal; // passed to fetch once you enable the call below\n  return toolDisabled(\"delete-album.webmcp.ts\");\n\n  // const data = await fetchDeleteAlbum(input, signal);\n  // return toolResult(data);\n}",
    "findings": [
      {
        "severity": "warning",
        "message": "No input field has a description. Constraints were synthesized from the schema; one line of prose per field in your spec or schema is better."
      }
    ]
  },
  {
    "name": "get-asset-info",
    "verb": "GET",
    "path": "/assets/{id}",
    "summary": "Retrieve an asset",
    "sideEffect": "read",
    "fileName": "get-asset-info.webmcp.ts",
    "fileCode": "import { getModelContext, callApi, toolResult, asToolError } from \"./runtime.webmcp\";\n\n// --- webmcp-codegen: generated. Do not edit this region. ---\n/**\n * Retrieve an asset\n *\n * Source: GET /assets/{id} (openapi). Risk: safe-read.\n * Starts enabled (see executeGetAssetInfo below).\n * Regenerate with: npx @webmcp-stack/codegen generate\n */\n\n/** The exact contract advertised to the agent. Derived from the API spec. Do not hand-edit. */\nexport const getAssetInfoInputSchema = {\n  \"type\": \"object\",\n  \"properties\": {\n    \"id\": {\n      \"type\": \"string\",\n      \"format\": \"uuid\",\n      \"description\": \"The unique identifier of the asset. A UUID.\"\n    }\n  },\n  \"required\": [\n    \"id\"\n  ]\n};\n\n/** What `execute` receives. The browser validates agent input against the schema above. */\nexport type GetAssetInfoInput = { \"id\": string };\n\n/** Safety hints computed by webmcp-codegen. Informational metadata for hosts and UIs. */\nexport const getAssetInfoHints = {\"readOnlyHint\":true,\"destructiveHint\":false,\"idempotentHint\":true,\"untrustedContentHint\":false} as const;\n\n/** The tool definition, minus `execute` (which is yours, below the marker). */\nexport const getAssetInfoTool = {\n  name: \"get-asset-info\",\n  title: \"Get Asset Info\",\n  description: \"Retrieve an asset\",\n  inputSchema: getAssetInfoInputSchema,\n  annotations: {\n    readOnlyHint: true,\n    untrustedContentHint: false,\n    consequentialHint: false,\n  },\n};\n\n/** The bare request, without the agent-facing result wrapping. Journeys\n * and your own code compose this; executeGetAssetInfo is the agent-facing one. */\nexport async function fetchGetAssetInfo(input: GetAssetInfoInput, signal?: AbortSignal) {\n  const data = await callApi(`/assets/${input.id}`, { method: \"GET\", signal });\n  return data;\n}\n\n/**\n * Register this tool with WebMCP. Call it once on page load, or use\n * registerAllTools() from the generated index.ts. Skips quietly when the\n * browser has no WebMCP runtime.\n *\n * Pass an AbortSignal to unregister later: controller.abort().\n */\nexport async function registerGetAssetInfo(signal?: AbortSignal): Promise<void> {\n  const modelContext = getModelContext();\n  if (!modelContext) return;\n  await modelContext.registerTool(\n    {\n      ...getAssetInfoTool,\n      // The browser has already validated the agent's input against the schema.\n      // A failure returns a readable result; it never throws (see asToolError).\n      execute: async (input, context) => {\n        try {\n          return await executeGetAssetInfo(input as GetAssetInfoInput, context?.signal);\n        } catch (error) {\n          return asToolError(error);\n        }\n      },\n    },\n    { signal },\n  );\n}\n\n// --- webmcp-codegen: end generated. Your code below survives regeneration. ---\n\n/**\n * What actually happens when the agent calls \"get-asset-info\".\n *\n * Default implementation: calls GET /assets/{id} from this page, with the\n * signed-in user's session. Replace it with your app's own API client\n * whenever you like; the contract above never changes.\n *\n * Calls the API on this page's own origin (same-origin by default).\n */\nexport async function executeGetAssetInfo(input: GetAssetInfoInput, signal?: AbortSignal) {\n  const data = await fetchGetAssetInfo(input, signal);\n  // Make the effect visible: an agent acts while a human watches this\n  // page. If this call changes what is on screen, update the UI here\n  // (navigate, invalidate a query, dispatch an event).\n  return toolResult(data);\n}",
    "findings": [
      {
        "severity": "warning",
        "message": "No output schema: the agent gets unstructured text back and has to guess at the shape. Add a response schema to the contract so agents can use the results reliably."
      },
      {
        "severity": "warning",
        "message": "No input field has a description. Constraints were synthesized from the schema; one line of prose per field in your spec or schema is better."
      }
    ]
  },
  {
    "name": "create-user-admin",
    "verb": "POST",
    "path": "/admin/users",
    "summary": "Create a user. Returns the user admin.",
    "sideEffect": "write",
    "fileName": "create-user-admin.webmcp.ts",
    "fileCode": "import { callApi, toolDisabled } from \"./runtime.webmcp\";\n\n// --- webmcp-codegen: generated. Do not edit this region. ---\n/**\n * Create a user. Returns the user admin.\n *\n * Source: POST /admin/users (openapi). Risk: write-confirm.\n * Starts withheld: not registered until you enable it (see registerCreateUserAdmin below).\n * Regenerate with: npx @webmcp-stack/codegen generate\n */\n\n/** The exact contract advertised to the agent. Derived from the API spec. Do not hand-edit. */\nexport const createUserAdminInputSchema = {\n  \"type\": \"object\",\n  \"properties\": {\n    \"email\": {\n      \"type\": \"string\",\n      \"format\": \"email\",\n      \"description\": \"An email address.\"\n    },\n    \"name\": {\n      \"type\": \"string\",\n      \"description\": \"Name.\"\n    },\n    \"password\": {\n      \"type\": \"string\",\n      \"description\": \"Password.\"\n    }\n  },\n  \"required\": [\n    \"email\",\n    \"name\",\n    \"password\"\n  ]\n};\n\n/** What `execute` receives. The browser validates agent input against the schema above. */\nexport type CreateUserAdminInput = { \"email\": string; \"name\": string; \"password\": string };\n\n/** Safety hints computed by webmcp-codegen. Informational metadata for hosts and UIs. */\nexport const createUserAdminHints = {\"readOnlyHint\":false,\"destructiveHint\":false,\"idempotentHint\":false,\"untrustedContentHint\":true} as const;\n\n/** The tool definition, minus `execute` (which is yours, below the marker). */\nexport const createUserAdminTool = {\n  name: \"create-user-admin\",\n  title: \"Create User Admin\",\n  description: \"Create a user. Returns the user admin.\",\n  inputSchema: createUserAdminInputSchema,\n  annotations: {\n    readOnlyHint: false,\n    untrustedContentHint: true,\n    consequentialHint: false,\n  },\n};\n\n/** The bare request, without the agent-facing result wrapping. Journeys\n * and your own code compose this; executeCreateUserAdmin is the agent-facing one. */\nexport async function fetchCreateUserAdmin(input: CreateUserAdminInput, signal?: AbortSignal) {\n  const data = await callApi(\"/admin/users\", { method: \"POST\", body: { email: input.email, name: input.name, password: input.password }, signal });\n  return data;\n}\n\n/**\n * Withheld: this tool is not registered, so agents cannot see or pick\n * it. The registration below stays commented until you enable the tool\n * (uncomment it and the body of executeCreateUserAdmin, or flip it in the\n * dashboard and regenerate).\n */\nexport async function registerCreateUserAdmin(signal?: AbortSignal): Promise<void> {\n  void signal;\n  // const modelContext = getModelContext();\n  // if (!modelContext) return;\n  //   await modelContext.registerTool(\n  //     {\n  //       ...createUserAdminTool,\n  //       execute: async (input, context) => {\n  //         // Cancellation wins over everything, including the confirmation.\n  //         context?.signal?.throwIfAborted();\n  //         // This tool changes things, so the user is always asked first. The\n  //         // confirmation lives in the generated region: it cannot be edited away.\n  //         const confirmed = await requestUserConfirmation(\n  //           \"Allow the agent to: Create a user. Returns the user admin.\",\n  //         );\n  //         if (!confirmed) {\n  //           return {\n  //             content: [{ type: \"text\", text: \"The user declined this action.\" }],\n  //             isError: true,\n  //           };\n  //         }\n  //         // The browser has already validated the agent's input against the schema.\n  //         // A failure returns a readable result; it never throws (see asToolError).\n  //         try {\n  //           return await executeCreateUserAdmin(input as CreateUserAdminInput, context?.signal);\n  //         } catch (error) {\n  //           return asToolError(error);\n  //         }\n  //       },\n  //     },\n  //     { signal },\n  //   );\n}\n\n// --- webmcp-codegen: end generated. Your code below survives regeneration. ---\n\n/**\n * What actually happens when the agent calls \"create-user-admin\".\n *\n * Default implementation: calls POST /admin/users from this page, with the\n * signed-in user's session. Replace it with your app's own API client\n * whenever you like; the contract above never changes.\n *\n * Calls the API on this page's own origin (same-origin by default).\n *\n * This tool is write-confirm: it changes things.\n * The user is asked to confirm every call (built into the generated region).\n */\n//\n// ! webmcp-codegen flagged these response fields as likely PII: email.\n// Everything you return reaches the agent. Leave those fields out of what you\n// return unless the agent genuinely needs them, and say so in a comment if you keep them.\nexport async function executeCreateUserAdmin(input: CreateUserAdminInput, signal?: AbortSignal) {\n  // This tool is withheld: nothing registers it, so agents cannot see\n  // or call it. To enable it, uncomment the request below and the\n  // registration above, and add toolResult to the import.\n  void signal; // passed to fetch once you enable the call below\n  return toolDisabled(\"create-user-admin.webmcp.ts\");\n\n  // const data = await fetchCreateUserAdmin(input, signal);\n  // return toolResult(data);\n}",
    "findings": [
      {
        "severity": "warning",
        "message": "Response may expose email. These fields reach the agent. Exclude them in execute() unless they are truly needed."
      },
      {
        "severity": "warning",
        "message": "Admin endpoint. It is generated disabled: exposing admin operations to agents should be a deliberate decision, reviewed endpoint by endpoint."
      },
      {
        "severity": "warning",
        "message": "No input field has a description. Constraints were synthesized from the schema; one line of prose per field in your spec or schema is better."
      }
    ]
  }
];

// Shaped like UiState from packages/codegen/src/dev/ui.ts; the landing demo
// injects it into the real dashboard UI.
export const DASHBOARD_STATE = {
  "label": "immich-excerpt.openapi.yaml",
  "outDir": "src/webmcp",
  "tools": [
    {
      "name": "get-all-albums",
      "description": "List all albums",
      "sideEffect": "read",
      "enabled": true,
      "endpointRole": "endpoint",
      "piiInOutput": [],
      "findings": [
        {
          "level": "warning",
          "message": "No output schema: the agent gets unstructured text back and has to guess at the shape. Add a response schema to the contract so agents can use the results reliably."
        }
      ],
      "inputSchema": {
        "type": "object",
        "properties": {
          "shared": {
            "type": "boolean",
            "description": "Only return shared albums"
          }
        },
        "required": []
      },
      "requiresAuth": false,
      "verb": "GET",
      "path": "/albums",
      "source": {
        "fileName": "get-all-albums.webmcp.ts",
        "code": "import { getModelContext, callApi, toolResult, asToolError } from \"./runtime.webmcp\";\n\n// --- webmcp-codegen: generated. Do not edit this region. ---\n/**\n * List all albums\n *\n * Source: GET /albums (openapi). Risk: safe-read.\n * Starts enabled (see executeGetAllAlbums below).\n * Regenerate with: npx @webmcp-stack/codegen generate\n */\n\n/** The exact contract advertised to the agent. Derived from the API spec. Do not hand-edit. */\nexport const getAllAlbumsInputSchema = {\n  \"type\": \"object\",\n  \"properties\": {\n    \"shared\": {\n      \"type\": \"boolean\",\n      \"description\": \"Only return shared albums\"\n    }\n  },\n  \"required\": []\n};\n\n/** What `execute` receives. The browser validates agent input against the schema above. */\nexport type GetAllAlbumsInput = { \"shared\"?: boolean };\n\n/** Safety hints computed by webmcp-codegen. Informational metadata for hosts and UIs. */\nexport const getAllAlbumsHints = {\"readOnlyHint\":true,\"destructiveHint\":false,\"idempotentHint\":true,\"untrustedContentHint\":false} as const;\n\n/** The tool definition, minus `execute` (which is yours, below the marker). */\nexport const getAllAlbumsTool = {\n  name: \"get-all-albums\",\n  title: \"Get All Albums\",\n  description: \"List all albums\",\n  inputSchema: getAllAlbumsInputSchema,\n  annotations: {\n    readOnlyHint: true,\n    untrustedContentHint: false,\n    consequentialHint: false,\n  },\n};\n\n/** The bare request, without the agent-facing result wrapping. Journeys\n * and your own code compose this; executeGetAllAlbums is the agent-facing one. */\nexport async function fetchGetAllAlbums(input: GetAllAlbumsInput, signal?: AbortSignal) {\n  const data = await callApi(\"/albums\", { method: \"GET\", query: { shared: input.shared }, signal });\n  return data;\n}\n\n/**\n * Register this tool with WebMCP. Call it once on page load, or use\n * registerAllTools() from the generated index.ts. Skips quietly when the\n * browser has no WebMCP runtime.\n *\n * Pass an AbortSignal to unregister later: controller.abort().\n */\nexport async function registerGetAllAlbums(signal?: AbortSignal): Promise<void> {\n  const modelContext = getModelContext();\n  if (!modelContext) return;\n  await modelContext.registerTool(\n    {\n      ...getAllAlbumsTool,\n      // The browser has already validated the agent's input against the schema.\n      // A failure returns a readable result; it never throws (see asToolError).\n      execute: async (input, context) => {\n        try {\n          return await executeGetAllAlbums(input as GetAllAlbumsInput, context?.signal);\n        } catch (error) {\n          return asToolError(error);\n        }\n      },\n    },\n    { signal },\n  );\n}\n\n// --- webmcp-codegen: end generated. Your code below survives regeneration. ---\n\n/**\n * What actually happens when the agent calls \"get-all-albums\".\n *\n * Default implementation: calls GET /albums from this page, with the\n * signed-in user's session. Replace it with your app's own API client\n * whenever you like; the contract above never changes.\n *\n * Calls the API on this page's own origin (same-origin by default).\n */\nexport async function executeGetAllAlbums(input: GetAllAlbumsInput, signal?: AbortSignal) {\n  const data = await fetchGetAllAlbums(input, signal);\n  // Make the effect visible: an agent acts while a human watches this\n  // page. If this call changes what is on screen, update the UI here\n  // (navigate, invalidate a query, dispatch an event).\n  return toolResult(data);\n}"
      }
    },
    {
      "name": "create-album",
      "description": "Create an album",
      "sideEffect": "write",
      "enabled": false,
      "endpointRole": "endpoint",
      "piiInOutput": [],
      "findings": [
        {
          "level": "warning",
          "message": "No input field has a description. Constraints were synthesized from the schema; one line of prose per field in your spec or schema is better."
        }
      ],
      "inputSchema": {
        "type": "object",
        "properties": {
          "albumName": {
            "type": "string",
            "description": "Album name."
          },
          "description": {
            "type": "string",
            "description": "Description."
          }
        },
        "required": [
          "albumName"
        ]
      },
      "requiresAuth": false,
      "verb": "POST",
      "path": "/albums",
      "source": {
        "fileName": "create-album.webmcp.ts",
        "code": "import { callApi, toolDisabled } from \"./runtime.webmcp\";\n\n// --- webmcp-codegen: generated. Do not edit this region. ---\n/**\n * Create an album\n *\n * Source: POST /albums (openapi). Risk: write-confirm.\n * Starts withheld: not registered until you enable it (see registerCreateAlbum below).\n * Regenerate with: npx @webmcp-stack/codegen generate\n */\n\n/** The exact contract advertised to the agent. Derived from the API spec. Do not hand-edit. */\nexport const createAlbumInputSchema = {\n  \"type\": \"object\",\n  \"properties\": {\n    \"albumName\": {\n      \"type\": \"string\",\n      \"description\": \"Album name.\"\n    },\n    \"description\": {\n      \"type\": \"string\",\n      \"description\": \"Description.\"\n    }\n  },\n  \"required\": [\n    \"albumName\"\n  ]\n};\n\n/** What `execute` receives. The browser validates agent input against the schema above. */\nexport type CreateAlbumInput = { \"albumName\": string; \"description\"?: string };\n\n/** Safety hints computed by webmcp-codegen. Informational metadata for hosts and UIs. */\nexport const createAlbumHints = {\"readOnlyHint\":false,\"destructiveHint\":false,\"idempotentHint\":false,\"untrustedContentHint\":false} as const;\n\n/** The tool definition, minus `execute` (which is yours, below the marker). */\nexport const createAlbumTool = {\n  name: \"create-album\",\n  title: \"Create Album\",\n  description: \"Create an album\",\n  inputSchema: createAlbumInputSchema,\n  annotations: {\n    readOnlyHint: false,\n    untrustedContentHint: false,\n    consequentialHint: false,\n  },\n};\n\n/** The bare request, without the agent-facing result wrapping. Journeys\n * and your own code compose this; executeCreateAlbum is the agent-facing one. */\nexport async function fetchCreateAlbum(input: CreateAlbumInput, signal?: AbortSignal) {\n  const data = await callApi(\"/albums\", { method: \"POST\", body: { albumName: input.albumName, description: input.description }, signal });\n  return data;\n}\n\n/**\n * Withheld: this tool is not registered, so agents cannot see or pick\n * it. The registration below stays commented until you enable the tool\n * (uncomment it and the body of executeCreateAlbum, or flip it in the\n * dashboard and regenerate).\n */\nexport async function registerCreateAlbum(signal?: AbortSignal): Promise<void> {\n  void signal;\n  // const modelContext = getModelContext();\n  // if (!modelContext) return;\n  //   await modelContext.registerTool(\n  //     {\n  //       ...createAlbumTool,\n  //       execute: async (input, context) => {\n  //         // Cancellation wins over everything, including the confirmation.\n  //         context?.signal?.throwIfAborted();\n  //         // This tool changes things, so the user is always asked first. The\n  //         // confirmation lives in the generated region: it cannot be edited away.\n  //         const confirmed = await requestUserConfirmation(\n  //           \"Allow the agent to: Create an album\",\n  //         );\n  //         if (!confirmed) {\n  //           return {\n  //             content: [{ type: \"text\", text: \"The user declined this action.\" }],\n  //             isError: true,\n  //           };\n  //         }\n  //         // The browser has already validated the agent's input against the schema.\n  //         // A failure returns a readable result; it never throws (see asToolError).\n  //         try {\n  //           return await executeCreateAlbum(input as CreateAlbumInput, context?.signal);\n  //         } catch (error) {\n  //           return asToolError(error);\n  //         }\n  //       },\n  //     },\n  //     { signal },\n  //   );\n}\n\n// --- webmcp-codegen: end generated. Your code below survives regeneration. ---\n\n/**\n * What actually happens when the agent calls \"create-album\".\n *\n * Default implementation: calls POST /albums from this page, with the\n * signed-in user's session. Replace it with your app's own API client\n * whenever you like; the contract above never changes.\n *\n * Calls the API on this page's own origin (same-origin by default).\n *\n * This tool is write-confirm: it changes things.\n * The user is asked to confirm every call (built into the generated region).\n */\nexport async function executeCreateAlbum(input: CreateAlbumInput, signal?: AbortSignal) {\n  // This tool is withheld: nothing registers it, so agents cannot see\n  // or call it. To enable it, uncomment the request below and the\n  // registration above, and add toolResult to the import.\n  void signal; // passed to fetch once you enable the call below\n  return toolDisabled(\"create-album.webmcp.ts\");\n\n  // const data = await fetchCreateAlbum(input, signal);\n  // return toolResult(data);\n}"
      }
    },
    {
      "name": "get-album-info",
      "description": "Retrieve an album",
      "sideEffect": "read",
      "enabled": true,
      "endpointRole": "endpoint",
      "piiInOutput": [],
      "findings": [
        {
          "level": "warning",
          "message": "No output schema: the agent gets unstructured text back and has to guess at the shape. Add a response schema to the contract so agents can use the results reliably."
        },
        {
          "level": "warning",
          "message": "No input field has a description. Constraints were synthesized from the schema; one line of prose per field in your spec or schema is better."
        }
      ],
      "inputSchema": {
        "type": "object",
        "properties": {
          "id": {
            "type": "string",
            "format": "uuid",
            "description": "The unique identifier of the album. A UUID."
          }
        },
        "required": [
          "id"
        ]
      },
      "requiresAuth": false,
      "verb": "GET",
      "path": "/albums/{id}",
      "source": {
        "fileName": "get-album-info.webmcp.ts",
        "code": "import { getModelContext, callApi, toolResult, asToolError } from \"./runtime.webmcp\";\n\n// --- webmcp-codegen: generated. Do not edit this region. ---\n/**\n * Retrieve an album\n *\n * Source: GET /albums/{id} (openapi). Risk: safe-read.\n * Starts enabled (see executeGetAlbumInfo below).\n * Regenerate with: npx @webmcp-stack/codegen generate\n */\n\n/** The exact contract advertised to the agent. Derived from the API spec. Do not hand-edit. */\nexport const getAlbumInfoInputSchema = {\n  \"type\": \"object\",\n  \"properties\": {\n    \"id\": {\n      \"type\": \"string\",\n      \"format\": \"uuid\",\n      \"description\": \"The unique identifier of the album. A UUID.\"\n    }\n  },\n  \"required\": [\n    \"id\"\n  ]\n};\n\n/** What `execute` receives. The browser validates agent input against the schema above. */\nexport type GetAlbumInfoInput = { \"id\": string };\n\n/** Safety hints computed by webmcp-codegen. Informational metadata for hosts and UIs. */\nexport const getAlbumInfoHints = {\"readOnlyHint\":true,\"destructiveHint\":false,\"idempotentHint\":true,\"untrustedContentHint\":false} as const;\n\n/** The tool definition, minus `execute` (which is yours, below the marker). */\nexport const getAlbumInfoTool = {\n  name: \"get-album-info\",\n  title: \"Get Album Info\",\n  description: \"Retrieve an album\",\n  inputSchema: getAlbumInfoInputSchema,\n  annotations: {\n    readOnlyHint: true,\n    untrustedContentHint: false,\n    consequentialHint: false,\n  },\n};\n\n/** The bare request, without the agent-facing result wrapping. Journeys\n * and your own code compose this; executeGetAlbumInfo is the agent-facing one. */\nexport async function fetchGetAlbumInfo(input: GetAlbumInfoInput, signal?: AbortSignal) {\n  const data = await callApi(`/albums/${input.id}`, { method: \"GET\", signal });\n  return data;\n}\n\n/**\n * Register this tool with WebMCP. Call it once on page load, or use\n * registerAllTools() from the generated index.ts. Skips quietly when the\n * browser has no WebMCP runtime.\n *\n * Pass an AbortSignal to unregister later: controller.abort().\n */\nexport async function registerGetAlbumInfo(signal?: AbortSignal): Promise<void> {\n  const modelContext = getModelContext();\n  if (!modelContext) return;\n  await modelContext.registerTool(\n    {\n      ...getAlbumInfoTool,\n      // The browser has already validated the agent's input against the schema.\n      // A failure returns a readable result; it never throws (see asToolError).\n      execute: async (input, context) => {\n        try {\n          return await executeGetAlbumInfo(input as GetAlbumInfoInput, context?.signal);\n        } catch (error) {\n          return asToolError(error);\n        }\n      },\n    },\n    { signal },\n  );\n}\n\n// --- webmcp-codegen: end generated. Your code below survives regeneration. ---\n\n/**\n * What actually happens when the agent calls \"get-album-info\".\n *\n * Default implementation: calls GET /albums/{id} from this page, with the\n * signed-in user's session. Replace it with your app's own API client\n * whenever you like; the contract above never changes.\n *\n * Calls the API on this page's own origin (same-origin by default).\n */\nexport async function executeGetAlbumInfo(input: GetAlbumInfoInput, signal?: AbortSignal) {\n  const data = await fetchGetAlbumInfo(input, signal);\n  // Make the effect visible: an agent acts while a human watches this\n  // page. If this call changes what is on screen, update the UI here\n  // (navigate, invalidate a query, dispatch an event).\n  return toolResult(data);\n}"
      }
    },
    {
      "name": "delete-album",
      "description": "Delete an album",
      "sideEffect": "destructive",
      "enabled": false,
      "endpointRole": "endpoint",
      "piiInOutput": [],
      "findings": [
        {
          "level": "warning",
          "message": "No input field has a description. Constraints were synthesized from the schema; one line of prose per field in your spec or schema is better."
        }
      ],
      "inputSchema": {
        "type": "object",
        "properties": {
          "id": {
            "type": "string",
            "format": "uuid",
            "description": "The unique identifier of the album. A UUID."
          }
        },
        "required": [
          "id"
        ]
      },
      "requiresAuth": false,
      "verb": "DELETE",
      "path": "/albums/{id}",
      "source": {
        "fileName": "delete-album.webmcp.ts",
        "code": "import { callApi, toolDisabled } from \"./runtime.webmcp\";\n\n// --- webmcp-codegen: generated. Do not edit this region. ---\n/**\n * Delete an album\n *\n * Source: DELETE /albums/{id} (openapi). Risk: destructive-confirm.\n * Starts withheld: not registered until you enable it (see registerDeleteAlbum below).\n * Regenerate with: npx @webmcp-stack/codegen generate\n */\n\n/** The exact contract advertised to the agent. Derived from the API spec. Do not hand-edit. */\nexport const deleteAlbumInputSchema = {\n  \"type\": \"object\",\n  \"properties\": {\n    \"id\": {\n      \"type\": \"string\",\n      \"format\": \"uuid\",\n      \"description\": \"The unique identifier of the album. A UUID.\"\n    }\n  },\n  \"required\": [\n    \"id\"\n  ]\n};\n\n/** What `execute` receives. The browser validates agent input against the schema above. */\nexport type DeleteAlbumInput = { \"id\": string };\n\n/** Safety hints computed by webmcp-codegen. Informational metadata for hosts and UIs. */\nexport const deleteAlbumHints = {\"readOnlyHint\":false,\"destructiveHint\":true,\"idempotentHint\":true,\"untrustedContentHint\":false} as const;\n\n/** The tool definition, minus `execute` (which is yours, below the marker). */\nexport const deleteAlbumTool = {\n  name: \"delete-album\",\n  title: \"Delete Album\",\n  description: \"Delete an album\",\n  inputSchema: deleteAlbumInputSchema,\n  annotations: {\n    readOnlyHint: false,\n    untrustedContentHint: false,\n    consequentialHint: true,\n  },\n};\n\n/** The bare request, without the agent-facing result wrapping. Journeys\n * and your own code compose this; executeDeleteAlbum is the agent-facing one. */\nexport async function fetchDeleteAlbum(input: DeleteAlbumInput, signal?: AbortSignal) {\n  const data = await callApi(`/albums/${input.id}`, { method: \"DELETE\", signal });\n  return data;\n}\n\n/**\n * Withheld: this tool is not registered, so agents cannot see or pick\n * it. The registration below stays commented until you enable the tool\n * (uncomment it and the body of executeDeleteAlbum, or flip it in the\n * dashboard and regenerate).\n */\nexport async function registerDeleteAlbum(signal?: AbortSignal): Promise<void> {\n  void signal;\n  // const modelContext = getModelContext();\n  // if (!modelContext) return;\n  //   await modelContext.registerTool(\n  //     {\n  //       ...deleteAlbumTool,\n  //       execute: async (input, context) => {\n  //         // Cancellation wins over everything, including the confirmation.\n  //         context?.signal?.throwIfAborted();\n  //         // This tool changes things, so the user is always asked first. The\n  //         // confirmation lives in the generated region: it cannot be edited away.\n  //         const confirmed = await requestUserConfirmation(\n  //           \"Allow the agent to: Delete an album\",\n  //         );\n  //         if (!confirmed) {\n  //           return {\n  //             content: [{ type: \"text\", text: \"The user declined this action.\" }],\n  //             isError: true,\n  //           };\n  //         }\n  //         // The browser has already validated the agent's input against the schema.\n  //         // A failure returns a readable result; it never throws (see asToolError).\n  //         try {\n  //           return await executeDeleteAlbum(input as DeleteAlbumInput, context?.signal);\n  //         } catch (error) {\n  //           return asToolError(error);\n  //         }\n  //       },\n  //     },\n  //     { signal },\n  //   );\n}\n\n// --- webmcp-codegen: end generated. Your code below survives regeneration. ---\n\n/**\n * What actually happens when the agent calls \"delete-album\".\n *\n * Default implementation: calls DELETE /albums/{id} from this page, with the\n * signed-in user's session. Replace it with your app's own API client\n * whenever you like; the contract above never changes.\n *\n * Calls the API on this page's own origin (same-origin by default).\n *\n * This tool is destructive-confirm: it cannot easily be undone.\n * The user is asked to confirm every call (built into the generated region).\n */\nexport async function executeDeleteAlbum(input: DeleteAlbumInput, signal?: AbortSignal) {\n  // This tool is withheld: nothing registers it, so agents cannot see\n  // or call it. To enable it, uncomment the request below and the\n  // registration above, and add toolResult to the import.\n  void signal; // passed to fetch once you enable the call below\n  return toolDisabled(\"delete-album.webmcp.ts\");\n\n  // const data = await fetchDeleteAlbum(input, signal);\n  // return toolResult(data);\n}"
      }
    },
    {
      "name": "get-asset-info",
      "description": "Retrieve an asset",
      "sideEffect": "read",
      "enabled": true,
      "endpointRole": "endpoint",
      "piiInOutput": [],
      "findings": [
        {
          "level": "warning",
          "message": "No output schema: the agent gets unstructured text back and has to guess at the shape. Add a response schema to the contract so agents can use the results reliably."
        },
        {
          "level": "warning",
          "message": "No input field has a description. Constraints were synthesized from the schema; one line of prose per field in your spec or schema is better."
        }
      ],
      "inputSchema": {
        "type": "object",
        "properties": {
          "id": {
            "type": "string",
            "format": "uuid",
            "description": "The unique identifier of the asset. A UUID."
          }
        },
        "required": [
          "id"
        ]
      },
      "requiresAuth": false,
      "verb": "GET",
      "path": "/assets/{id}",
      "source": {
        "fileName": "get-asset-info.webmcp.ts",
        "code": "import { getModelContext, callApi, toolResult, asToolError } from \"./runtime.webmcp\";\n\n// --- webmcp-codegen: generated. Do not edit this region. ---\n/**\n * Retrieve an asset\n *\n * Source: GET /assets/{id} (openapi). Risk: safe-read.\n * Starts enabled (see executeGetAssetInfo below).\n * Regenerate with: npx @webmcp-stack/codegen generate\n */\n\n/** The exact contract advertised to the agent. Derived from the API spec. Do not hand-edit. */\nexport const getAssetInfoInputSchema = {\n  \"type\": \"object\",\n  \"properties\": {\n    \"id\": {\n      \"type\": \"string\",\n      \"format\": \"uuid\",\n      \"description\": \"The unique identifier of the asset. A UUID.\"\n    }\n  },\n  \"required\": [\n    \"id\"\n  ]\n};\n\n/** What `execute` receives. The browser validates agent input against the schema above. */\nexport type GetAssetInfoInput = { \"id\": string };\n\n/** Safety hints computed by webmcp-codegen. Informational metadata for hosts and UIs. */\nexport const getAssetInfoHints = {\"readOnlyHint\":true,\"destructiveHint\":false,\"idempotentHint\":true,\"untrustedContentHint\":false} as const;\n\n/** The tool definition, minus `execute` (which is yours, below the marker). */\nexport const getAssetInfoTool = {\n  name: \"get-asset-info\",\n  title: \"Get Asset Info\",\n  description: \"Retrieve an asset\",\n  inputSchema: getAssetInfoInputSchema,\n  annotations: {\n    readOnlyHint: true,\n    untrustedContentHint: false,\n    consequentialHint: false,\n  },\n};\n\n/** The bare request, without the agent-facing result wrapping. Journeys\n * and your own code compose this; executeGetAssetInfo is the agent-facing one. */\nexport async function fetchGetAssetInfo(input: GetAssetInfoInput, signal?: AbortSignal) {\n  const data = await callApi(`/assets/${input.id}`, { method: \"GET\", signal });\n  return data;\n}\n\n/**\n * Register this tool with WebMCP. Call it once on page load, or use\n * registerAllTools() from the generated index.ts. Skips quietly when the\n * browser has no WebMCP runtime.\n *\n * Pass an AbortSignal to unregister later: controller.abort().\n */\nexport async function registerGetAssetInfo(signal?: AbortSignal): Promise<void> {\n  const modelContext = getModelContext();\n  if (!modelContext) return;\n  await modelContext.registerTool(\n    {\n      ...getAssetInfoTool,\n      // The browser has already validated the agent's input against the schema.\n      // A failure returns a readable result; it never throws (see asToolError).\n      execute: async (input, context) => {\n        try {\n          return await executeGetAssetInfo(input as GetAssetInfoInput, context?.signal);\n        } catch (error) {\n          return asToolError(error);\n        }\n      },\n    },\n    { signal },\n  );\n}\n\n// --- webmcp-codegen: end generated. Your code below survives regeneration. ---\n\n/**\n * What actually happens when the agent calls \"get-asset-info\".\n *\n * Default implementation: calls GET /assets/{id} from this page, with the\n * signed-in user's session. Replace it with your app's own API client\n * whenever you like; the contract above never changes.\n *\n * Calls the API on this page's own origin (same-origin by default).\n */\nexport async function executeGetAssetInfo(input: GetAssetInfoInput, signal?: AbortSignal) {\n  const data = await fetchGetAssetInfo(input, signal);\n  // Make the effect visible: an agent acts while a human watches this\n  // page. If this call changes what is on screen, update the UI here\n  // (navigate, invalidate a query, dispatch an event).\n  return toolResult(data);\n}"
      }
    },
    {
      "name": "create-user-admin",
      "description": "Create a user. Returns the user admin.",
      "sideEffect": "write",
      "enabled": false,
      "endpointRole": "admin",
      "piiInOutput": [
        "email"
      ],
      "findings": [
        {
          "level": "warning",
          "message": "Response may expose email. These fields reach the agent. Exclude them in execute() unless they are truly needed."
        },
        {
          "level": "warning",
          "message": "Admin endpoint. It is generated disabled: exposing admin operations to agents should be a deliberate decision, reviewed endpoint by endpoint."
        },
        {
          "level": "warning",
          "message": "No input field has a description. Constraints were synthesized from the schema; one line of prose per field in your spec or schema is better."
        }
      ],
      "inputSchema": {
        "type": "object",
        "properties": {
          "email": {
            "type": "string",
            "format": "email",
            "description": "An email address."
          },
          "name": {
            "type": "string",
            "description": "Name."
          },
          "password": {
            "type": "string",
            "description": "Password."
          }
        },
        "required": [
          "email",
          "name",
          "password"
        ]
      },
      "requiresAuth": false,
      "verb": "POST",
      "path": "/admin/users",
      "source": {
        "fileName": "create-user-admin.webmcp.ts",
        "code": "import { callApi, toolDisabled } from \"./runtime.webmcp\";\n\n// --- webmcp-codegen: generated. Do not edit this region. ---\n/**\n * Create a user. Returns the user admin.\n *\n * Source: POST /admin/users (openapi). Risk: write-confirm.\n * Starts withheld: not registered until you enable it (see registerCreateUserAdmin below).\n * Regenerate with: npx @webmcp-stack/codegen generate\n */\n\n/** The exact contract advertised to the agent. Derived from the API spec. Do not hand-edit. */\nexport const createUserAdminInputSchema = {\n  \"type\": \"object\",\n  \"properties\": {\n    \"email\": {\n      \"type\": \"string\",\n      \"format\": \"email\",\n      \"description\": \"An email address.\"\n    },\n    \"name\": {\n      \"type\": \"string\",\n      \"description\": \"Name.\"\n    },\n    \"password\": {\n      \"type\": \"string\",\n      \"description\": \"Password.\"\n    }\n  },\n  \"required\": [\n    \"email\",\n    \"name\",\n    \"password\"\n  ]\n};\n\n/** What `execute` receives. The browser validates agent input against the schema above. */\nexport type CreateUserAdminInput = { \"email\": string; \"name\": string; \"password\": string };\n\n/** Safety hints computed by webmcp-codegen. Informational metadata for hosts and UIs. */\nexport const createUserAdminHints = {\"readOnlyHint\":false,\"destructiveHint\":false,\"idempotentHint\":false,\"untrustedContentHint\":true} as const;\n\n/** The tool definition, minus `execute` (which is yours, below the marker). */\nexport const createUserAdminTool = {\n  name: \"create-user-admin\",\n  title: \"Create User Admin\",\n  description: \"Create a user. Returns the user admin.\",\n  inputSchema: createUserAdminInputSchema,\n  annotations: {\n    readOnlyHint: false,\n    untrustedContentHint: true,\n    consequentialHint: false,\n  },\n};\n\n/** The bare request, without the agent-facing result wrapping. Journeys\n * and your own code compose this; executeCreateUserAdmin is the agent-facing one. */\nexport async function fetchCreateUserAdmin(input: CreateUserAdminInput, signal?: AbortSignal) {\n  const data = await callApi(\"/admin/users\", { method: \"POST\", body: { email: input.email, name: input.name, password: input.password }, signal });\n  return data;\n}\n\n/**\n * Withheld: this tool is not registered, so agents cannot see or pick\n * it. The registration below stays commented until you enable the tool\n * (uncomment it and the body of executeCreateUserAdmin, or flip it in the\n * dashboard and regenerate).\n */\nexport async function registerCreateUserAdmin(signal?: AbortSignal): Promise<void> {\n  void signal;\n  // const modelContext = getModelContext();\n  // if (!modelContext) return;\n  //   await modelContext.registerTool(\n  //     {\n  //       ...createUserAdminTool,\n  //       execute: async (input, context) => {\n  //         // Cancellation wins over everything, including the confirmation.\n  //         context?.signal?.throwIfAborted();\n  //         // This tool changes things, so the user is always asked first. The\n  //         // confirmation lives in the generated region: it cannot be edited away.\n  //         const confirmed = await requestUserConfirmation(\n  //           \"Allow the agent to: Create a user. Returns the user admin.\",\n  //         );\n  //         if (!confirmed) {\n  //           return {\n  //             content: [{ type: \"text\", text: \"The user declined this action.\" }],\n  //             isError: true,\n  //           };\n  //         }\n  //         // The browser has already validated the agent's input against the schema.\n  //         // A failure returns a readable result; it never throws (see asToolError).\n  //         try {\n  //           return await executeCreateUserAdmin(input as CreateUserAdminInput, context?.signal);\n  //         } catch (error) {\n  //           return asToolError(error);\n  //         }\n  //       },\n  //     },\n  //     { signal },\n  //   );\n}\n\n// --- webmcp-codegen: end generated. Your code below survives regeneration. ---\n\n/**\n * What actually happens when the agent calls \"create-user-admin\".\n *\n * Default implementation: calls POST /admin/users from this page, with the\n * signed-in user's session. Replace it with your app's own API client\n * whenever you like; the contract above never changes.\n *\n * Calls the API on this page's own origin (same-origin by default).\n *\n * This tool is write-confirm: it changes things.\n * The user is asked to confirm every call (built into the generated region).\n */\n//\n// ! webmcp-codegen flagged these response fields as likely PII: email.\n// Everything you return reaches the agent. Leave those fields out of what you\n// return unless the agent genuinely needs them, and say so in a comment if you keep them.\nexport async function executeCreateUserAdmin(input: CreateUserAdminInput, signal?: AbortSignal) {\n  // This tool is withheld: nothing registers it, so agents cannot see\n  // or call it. To enable it, uncomment the request below and the\n  // registration above, and add toolResult to the import.\n  void signal; // passed to fetch once you enable the call below\n  return toolDisabled(\"create-user-admin.webmcp.ts\");\n\n  // const data = await fetchCreateUserAdmin(input, signal);\n  // return toolResult(data);\n}"
      }
    }
  ],
  "skipped": [],
  "notes": []
};
