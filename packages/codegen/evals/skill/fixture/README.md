# beenthere-lite (eval fixture)

A memory-journal app for trips you've *been on* - deliberately NOT a travel
planner. Generated by `@webmcp-stack/codegen generate --spec spec.json` and
committed as the baseline every eval case starts from.

Refresh after template changes: re-run the command above into a scratch dir
and copy the result over this folder.
